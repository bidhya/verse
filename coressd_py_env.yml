name:
channels:
  - conda-forge
  - default
dependencies:
  - geopandas
  - rioxarray
  - libgdal-hdf4 # needed separate install lately for reading hdf files in rioxarray 
  - libgdal-hdf5 # for nc file that may have been created using hdf5 protocol; again for rioxarray  
  - netCDF4  # seems now required for opening nc file with xarray
  - h5netcdf
  - dask
  - papermill
  - hvplot
  - geoviews
  - ipykernel
  #- pyhdf  # was used initially with codes from mhanama
  